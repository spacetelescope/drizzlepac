[build-system]
requires = [
    "setuptools>=38.2.5",
    "wheel",
    "numpy",
    "astropy"
]

[tool.stsci-bot]
check_milestone = false

[build_system]
requires = ["setuptools", "wheel", "astropy", "numpy", "sphinx"]
