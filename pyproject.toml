[build-system]
requires = ["setuptools>=42.0",
            "setuptools_scm[toml]>=3.4",
            "wheel",
            "numpy>=1.18",
            "astropy>=5.0.4",
            "markupsafe<=2.0.1"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
